import React from 'react';

const ProgressBar = () => {
    // Progress logic would be implemented based on time or completion
    return (
        <div className="progress-bar">
            <div className="progress" style={{ width: '0%' }}></div>
        </div>
    );
};

export default ProgressBar;